1. Сборка лежит в папке build

2. Исходники в src

3. Для сборки проекта запустить:

`npm i`

`bower i jquery` (в этом проекте скрипты не используются, это только для того, чтобы проект собрался без ошибок)

`gulp default`

